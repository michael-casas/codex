@CDX-WF-BUN-REACT-RED-GREEN
Feature: Three-stage Bun and Vite React RED to GREEN workflow
  Rule: Real implementation evidence must flow through an immutable independent audit

    Scenario: BUN-REACT-RED-GREEN-L3-001 Build audit remediate and stop ready for external audit
      Given the canonical Bun React RED to GREEN workflow and isolated proof root are admitted
      When the workflow executes through its literal installed shebang with the live backend
      Then exactly three sequential Luna low stages complete as builder auditor and remediator
      And the pinned Bun and Vite React baseline receives an immutable independent RED audit
      And the remediator consumes that RED makes the fixed audit GREEN and stops READY_FOR_EXTERNAL_AUDIT
      And the journal report generated source tree and resource delta are complete and bounded
