# CAS-RP-01 amendment 04 — Founder defers named profiles

Founder ruling: defer named model/configuration profiles and prefer explicit
model/effort combinations approved by the Founder through execution assignments.

This supersedes the named-profile completion requirement in
CAS-RP-01-PROFILES-ADDENDUM.md. That addendum and the synthetic native probe remain
historical evidence, not active implementation requirements. No profile loader,
native-profile adapter, remote profile selection, or protocol refresh is required
or authorized by this repair.

CAS-RP-01 returns to its original explicit runtime-profile objective:

- Preserve explicit per-agent model and effort through authoring, MCP input,
  validation, frozen identities, persistence/replay and actual App Server requests.
- Require authorized execution settings; provider catalog availability is not
  authority to launch. Do not add a global hard-coded two-model allowlist: future
  Founder-approved combinations must be representable without another policy rewrite.
- Reject malformed or provider-unsupported combinations without silent defaults,
  downgrade, substitution or inherited unapproved settings.
- Current demo approval is four gpt-5.6-luna/high builders followed by one
  gpt-5.6-sol/medium design judge. Do not launch them from this repair worker.
- Preserve existing sandbox, approval, security-role routing and resource limits.

Preserve previous explicit-profile contract rows and meaningful RED/GREEN.
Disposition only added named-profile rows as Founder-deferred in a versioned
contract amendment; do not delete failing explicit-profile tests or claim the
profile probe passed. The native rejection remains documented deferred evidence.

AGENT_RUNTIME_PROFILE_READY may be emitted when all explicit model/effort gates,
affected checks and cleanup pass. Report remaining installed-plugin deployment
or discovery obligations separately. Named-profile support no longer blocks this
terminal. File lease, worker identity, no audit/commit/deploy and no remote/home
mutation boundaries remain unchanged.
