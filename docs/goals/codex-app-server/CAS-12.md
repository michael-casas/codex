# CAS-12 Goal Charter — Integrated Recovery and Dogfood

> **Superseded on 2026-09-03.** ChatGPT website/Developer Mode/tunnel dogfood is no longer a Base requirement. Canonical Codex desktop dogfood is owned by `CAS-12-R1-CODEX-DOGFOOD.md`.

**Model:** `gpt-5.6-sol`, medium reasoning  
**Role:** Base integration owner for restart-safe local/remote operation  
**State:** allocated; non-dispatchable until CAS-11 `PLUGIN_READY` and every operational predecessor terminal  
**Lease:** `CAS-12-INTEGRATED-DOGFOOD`  
**Terminal:** `BASE_READY`, `READY-FOR-AUDIT`, or an exact dependency halt

## Objective

Prove the accepted control plane as one restart-safe system through public surfaces: local and read-only remote handoff, workflow execution, messaging/reply, blocked decisions, selected-agent visibility, ChatGPT plugin rendering, daemon/App Server restart, and remote disconnect/reconnect—without duplicate durable effects, false acceptance, secret leakage, or resource loss.

Dogfood the frozen presentation descriptor end to end: idempotent local auto-open intent for the Codex in-app Browser, the same Svelte bundle through MCP Apps, and one authenticated read-only mobile HTTPS view. Prove denial, reconnect, no duplicate tab/link, and no-secret URL behavior. Tailscale Serve is canonical private ingress; Cloudflare Tunnel is admissible only with Cloudflare Access. Actual durable Synology deployment remains deferred until a later Founder authorization.

CAS-12 also owns the explicit external real-ChatGPT boundary deferred by CAS-11: developer-mode plugin registration and connection of the exact accepted installable artifact, followed by selected-feed rendering and model-readable fallback evidence. If account/environment access is unavailable, halt with the exact external capability and resume condition; do not relabel isolated Codex or Playwright host evidence as ChatGPT.

## Required context

Read repository authority, BATDD profile/contract, CAS-12 assignment, exact CAS-02/CAS-04/CAS-05/CAS-06/CAS-07/CAS-08/CAS-11 packets, and current official App Server, MCP Apps UI, plugin testing, and tunnel documentation. Invoke `seam`, `batdd`, `nx-monorepo`, `workflows`, `data-substrate`, `openai-docs`, `impeccable` for rendered evidence, and full `ponytail:ponytail` before writes.

## Dependency gate

Remain non-dispatchable until `HOST_TRANSPORT_READY`, `WORKSPACE_READY`, `MESSAGING_READY`, `HANDOFF_READY`, `REMOTE_WORKFLOW_READY`, `VISIBILITY_PROJECTION_READY`, and `PLUGIN_READY` are verified and materialized in shared `a4a6`. Do not replace a missing boundary with a test-only authority or hidden fallback.

The worker must run as one same-directory task whose `pwd` is exactly `/Users/mcasa_atlantis/.codex/worktrees/a4a6/.codex`. It owns the sole `CAS-12-INTEGRATED-DOGFOOD` lease, must not launch descendants or replacements, and must route root/shared composition or unavailable external ChatGPT connection facts to coordinator `01a05dd0-05d5-7a33-b3fa-66adf9e41d8c` rather than requesting interactive approval or widening its surface.

## Authorized write surface

- `apps/codex-control-e2e/features/cas-12/**`
- `apps/codex-control-e2e/src/cas-12/**`
- `packages/testing/evidence/cas-12-*.json`
- `.agent/testing/cas-12/**`

All product implementations are read-only accepted predecessors. Shared E2E configuration, root files, fixtures used by another seam, or product repairs require an immutable amendment and the owning Base lane.

## Green Contract floor

- Canonical flows: local handoff; authorized read-only remote handoff/observation; remote workflow placement without live mutation; local-to-remote message/reply; blocked approval/user input; selected-agent feed; plugin render/fallback; daemon restart; local App Server restart; remote reconnect.
- One accepted command yields one stable handle and one legal durable transition. No duplicate agent, turn, message, artifact, queue job, workspace lease, or completion state after retry/restart.
- Runtime terminal, quiet output, transport close, UI render, or monitor wake never implies workflow/feature acceptance.
- Preserve exact model/reasoning, assignment digest, sandbox, approval, host, workspace, and completion boundary identities.
- Prove ambiguous disconnect reconciliation before retry and failed-only continuation after restart.
- Public events/UI omit prompts, raw inputs, credentials, environment, reasoning, unnecessary paths, and unbounded command output.
- Capture exact base/candidate identity, protocol digest/version, host identities, selected/executed counts, MCP trace, Playwright/Cucumber reports, PostgreSQL/pg-boss inventory, and before/after resource delta.
- Live Synology remains strictly read-only. Use controlled authenticated WSS for mutating dogfood; fail closed if the scenario would mutate the live host.
- One local auto-open intent creates or focuses at most one route for a handle across retry, reconnect, and restart; it does not create duplicate tabs.
- One authenticated mobile view is read-only, rejects unauthenticated/unauthorized access, preserves handle identity, reconnects without duplicate links, and never exposes a secret-bearing URL.
- Browser and MCP Apps clients reach only the local control/visibility gateway and never remote App Server WSS. Mobile ingress terminates authenticated HTTPS before that same gateway.
- Tailscale Serve is canonical for private mobile ingress. A Cloudflare Tunnel without Cloudflare Access is a hard denial. Do not deploy persistent Synology state under Base read-only authority.
- Physical Gherkin owns representative integrated scenarios. Native L2 owns restart, duplication, failure, cleanup, and resource matrices.
- Real ChatGPT developer-mode dogfood binds the exact CAS-11 artifact digest, registration/connection identity, selected-feed interaction, fallback, and cleanup. It may use only an explicitly authorized authenticated connection surface and cannot introduce public publishing or an unauthenticated tunnel.
- Completion means `BASE_READY` evidence only; it does not grant audit acceptance or authorize CAS-13.

## Task budget

Each canonical intent uses its accepted one-call API and returned stable handle. Integrated dogfood must reject caller-visible provider choreography and report actual call counts/result sizes.

## Stop and handoff

Stop at `BASE_READY` / `READY-FOR-AUDIT`, or `HALT` with exact missing external capability and resume condition. CAS-13, audit, QA, verification, Final Polish, commit, push, publication, cleanup beyond owned fixtures, and live remote mutation remain prohibited.
