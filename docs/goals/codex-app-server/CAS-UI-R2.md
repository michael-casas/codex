# CAS-UI-R2 — readable Svelte AI Elements evaluation

Role: implementation owner. Founder authorizes evaluation WITH implementation of the approved discovery plan; stop READY-FOR-VERIFICATION (assignment vocabulary READY-FOR-AUDIT), never self-accept BASE_READY.
Workspace: /Users/mcasa_atlantis/.codex/worktrees/a4a6/.codex. Preserve dirty work and all historical CAS-UI-R1 contracts/evidence. Coordinator owns live migrations, service restart authorization and dogfood cancellation/cessation.

## Outcome and authority

Implement human workflow titles/fallback/containment; stable typed readable messages and safe validated results/compact tools; bounded scroll preserving older-content position with jump-to-latest; newly appended text reveal and reduced motion; authoritative cancelled active-view removal, selected-feed teardown and exact known-empty `No Workflows Running`. Preserve other workflows, offline uncertainty and history.

Evaluate community upstream https://github.com/SikandarJODD/ai-elements at immutable commit fa4bc217f84bc571378bc371332a154106772614. Read actual Message/Conversation/Tool/Response source and MIT license. Verify exact streaming Markdown dependency and safe configuration before proposing installation. Select only justified component code/behavior and preserve license notices; no bulk registry installation.

Maintain Svelte, current OpenAI-aligned styles and Codex App Server backend. No React, ChatKit backend, Vercel inference SDK, animation framework, alternative runtime or speculative renderer registry. The existing one-call agent-facing API remains simple.

## Required execution

Read AGENTS/GUIDELINES/README, current BATDD skill/profile/worker contract, seam implementation, File System/Clean Code, relevant Nx routing and UI skill guidance. Reuse prior discovery plan/catalog without repeating broad research. Compile/validate assignment and execution plan. Define meaningful RED for every new/changed contract row, freeze new acceptance, then product GREEN. Old frozen tests remain intact except separately approved harness-only adaptations.

Maximum one bounded nonrecursive child with durable charter/disjoint files. Parent owns shared schema/composition/contract writes. All new dependencies and root lock changes go through exact coordinator integration. Never silently widen file scope.

## Interfaces and invariants

- Stable item ownership includes run/delegation, agent, host/thread/turn/item; event replay identity/revision is distinct. Mismatched turn events cannot update another active turn.
- Normalized item metadata preserves kind/lifecycle/nullable message phase and truthful truncation. Deltas append once; completed item replaces; late events do not resurrect finalized content; structured-result descriptor is produced only after existing outputSchema validation and binds its final item.
- A bounded logical-item read model lives in existing visibility ownership; no second scheduler/event authority. Migration007 is additive; live-applied006 is not edited. Old chunk limits/history remain explicit.
- Proposed64KiB/item,100 logical items,2MiB selected aggregate are evaluation limits only until coordinator disposition. Label them and show truncation; do not silently prune durable evidence.
- Only selected-agent details cross the browser boundary. Connection status, item/result payload status, authoritative workflow state and confirmed cessation remain distinct.
- Cancel ACK/disconnect/unknown state are not cancellation. Terminal cancellation hides current-work card/feed and reconciles direct route; other active runs remain; unknown/incomplete/offline scopes cannot claim global emptiness.
- Safe Markdown disables raw HTML and dangerous links/media. Never expose private reasoning, environment, raw tool arguments or credentials. Paths/ports are not automatically trusted links.

## Validation

L1: ownership/replay/final replacement/Unicode/clipping/privacy/caps; title metadata and identity preservation; current-work cancellation/unknown/scope policy; component data adapters and safe rendering.
L2: isolated PostgreSQL migration/item persistence/reconnect/retention/selection; existing schema-validation output binding; compiled source title admission; safe HTTP contracts.
L3: physical Gherkin lowered to native desktop/mobile Playwright against synthetic/replay production composition; title overflow, message/result/tool display, bounded scrolling/jump/focus/reduced motion, authoritative cancellation/multiple runs/late events/offline. Nonzero evidence and explicit child/process/socket/database/temp cleanup.

Maintain real in-app Browser feedback beside this task; inspect actual app and bounded evaluation data. Do not shut down shared viewer during fixture cleanup. Before any necessary shared daemon rebuild/restart coordinate exact safe action. No paid model turns, dogfood retry, live migration, extra audits, commits, merges, pushes or deployment.

## Handoff

Record upstream commit/license/selected code and exact dependencies, baseline versus candidate emitted bundle bytes, native results and safe scoped resource inventory. Distinguish evaluation from permanent policy and remaining compatibility limits. Stop READY-FOR-VERIFICATION or an exact authority/dependency blocker.
